from setuptools import find_packages, setup

setup(
    name="diamonds-price-prediction",
    version='2.0.0',
    packages=find_packages(),
    author="Enes Guler",
    description="MLOps pipeline with MLflow Registry for Diamonds dataset.",
)